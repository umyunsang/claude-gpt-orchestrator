---
description: Show the observable Codex specialist job status for the current project
argument-hint: "[job-id]"
---

Call the status tool from the cgo MCP server. If $ARGUMENTS contains exactly one job ID, pass it as job_id; if it is empty, pass no arguments. Reject any other form. Summarize job ID, status, phase, and remaining observability gaps.
